{
    "name": "scrimba-projects",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "build:hometown-homepage": "npm --prefix hometown-homepage run build",
        "build:basketball-scoreboard": "npm --prefix basketball-scoreboard run build",
        "build:password-generator": "npm --prefix password-generator run build",
        "build:unit-converter": "npm --prefix unit-converter run build",
        "build:oldagram": "npm --prefix oldagram run build",
        "build:restaurant-ordering": "npm --prefix restaurant-ordering run build",
        "build:learning-blog": "npm --prefix learning-blog run build"
    },
    "dependencies": {
        "firebase": "^10.4.0",
        "node-fetch": "^3.3.1"
    }
}
